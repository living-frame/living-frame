# מסגרת חיה — מסמך המשך

מסמך אחד שמכיל את כל מה שצריך כדי להמשיך את הפרויקט בשיחה חדשה.
מי שקורא אותו — בן אדם או מודל — אמור להבין את המערכת בלי לחזור אחורה.

**גרסה נוכחית:** 1.0.8 · **נוצר ע״י אמיר כהן** · בית כנסת הרומני, גדרה

---

## 1. מה זה

אפליקציית ריאליטי רבוד לטלפון. מכוונים את המצלמה לתמונה שתלויה על קיר בית הכנסת,
המערכת מזהה אותה, ומריצה עליה סרטון הנפשה שנצמד למסגרת — באותו גודל ובאותה זווית,
גם כשהתמונה קטנה בפריים או נצפית מהצד.

התמונה הראשונה שנבנתה סביבה: פוסטר "יהודה" עם האריה, מוקדש לעילוי נשמת שמעון שמש בן רחל ויוסף ז״ל.
נוספה לאחר מכן "זבולון" עם הספינה.

**הכתובות:**

| | |
|---|---|
| האפליקציה למתפללים | https://living-frame.github.io/living-frame/ |
| מסך הגבאי | https://living-frame.github.io/living-frame/admin.html |
| הריפו | https://github.com/living-frame/living-frame (ציבורי, ענף main, Pages מ-root) |

---

## 2. ארכיטקטורה

```
גיטהאב  ── manifest.json    רשימת הפריטים: שם, מיקום, הקדשה, שמות קבצים, rev, ציון זיהוי
        ── targets.mind     כל תמונות המטרה דחוסות לקובץ זיהוי אחד
        ── media/           התמונות והסרטונים
             ▲                        │
             │ כותב בפרסום            │ מוריד לפי דרישה
        admin.html                index.html
        (מסך גבאי)                (הטלפון)
```

**אין שרת ואין בילד.** גיטהאב הוא גם האחסון וגם מסד הנתונים. הכול קבצים סטטיים.

**בטלפון:** בפתיחה יורדים רק `manifest.json` ו-`targets.mind` (כ-800 ק״ב).
סרטון יורד רק בפעם הראשונה שהתמונה שלו זוהתה, ואז נשמר כ-Blob ב-IndexedDB
(מסד `living-frame-media`, מפתח `id@rev`). בפעם הבאה הוא נפתח מיידית וגם בלי רשת.
`rev` משתנה כשמחליפים מדיה, ואז הקליפ יורד מחדש והישן נמחק.

**במסך הגבאי:** אין מאגר מקומי. הוא קורא את `manifest.json` מגיטהאב דרך ה-API,
ובפרסום מעלה מדיה חדשה, מוריד את תמונות המקור הקיימות, מהדר מחדש את `targets.mind`
בתוך הדפדפן, וכותב `manifest.json` מעודכן. רק פריטים שנוספו ועדיין לא פורסמו
נשמרים זמנית ב-IndexedDB (`living-frame-drafts`), כדי שרענון לא יאבד סרטון שנגרר.

---

## 3. הקבצים

| קובץ | תפקיד |
|---|---|
| `index.html` | האפליקציה. סצנת A-Frame נבנית דינמית מתוך ה-manifest |
| `admin.html` | מסך הגבאי: כניסה, הוספה, בדיקת איכות, פרסום |
| `sw.js` | Service worker. שם המטמון נושא את מספר הגרסה |
| `manifest.json` | רשימת הפריטים + `cdn` + `maxTrack` |
| `targets.mind` | מאגר הזיהוי המהודר |
| `media/` | `lion.jpg`, `lion.mp4`, ופריטים שנוספו בשמות `it<base36>` |
| `logo.png` / `logo-dark.png` | הלוגו לרקע בהיר / כהה |
| `og.jpg` | כרטיס 1200×630 לתצוגה מקדימה בוואטסאפ |
| `app.webmanifest` | הוספה למסך הבית |
| `compile.mjs` | בניית `targets.mind` משורת פקודה, חלופה למסך הגבאי |
| `README.md` | הוראות התקנה ותפעול |

---

## 4. ספריות והחלטות טכניות

* **MindAR 1.2.5 + A-Frame 1.5.0** מ-CDN. הנתיב `mindar-image-aframe.prod.js` נטען בתג script רגיל.
* **המהדר במסך הגבאי הוא `mindar-image.prod.js`, והוא מודול ES.** תג script רגיל לא מריץ אותו כלל
  ומשאיר את `window.MINDAR` ריק. נטען בייבוא דינמי, עם unpkg כגיבוי. ה-Worker שלו ארוז כ-blob פנימי,
  ולכן טעינה חוצת־מקורות עובדת.
* **הצמדת הווידאו:** המישור נבנה ברוחב 1 וגובה `h/w` של התמונה המודפסת. הקומפוננטה `fit-video`
  מחשבת `repeat`/`offset` על טקסטורת הווידאו כדי לחתוך ליחס במקום למתוח. כפתור בממשק מחליף למתיחה
  מלאה לצורך השוואה.
* **הידור:** כ-15 שניות לתמונה, ורץ על כל המאגר בכל פרסום, כי `targets.mind` הוא קובץ אחד.
* **ציון איכות:** סכום נקודות המעקב מ-`trackingData`. מעל 40 יציב, 20–40 גבולי, מתחת ל-20 לא מומלץ.
  לשם כיול: האריה 72–73, זבולון 74.
* **`maxTrack`** קובע כמה תמונות מתנגנות בו־זמנית. ברירת מחדל 2. כל תמונה נוספת עולה בקצב פריימים.
* **מגבלות סרטון:** עד דקה וחצי ועד 30 מגה, נאכף במסך הגבאי. הבדיקה האמיתית היא קצב הקידוד —
  מעל 2 מגהביט לשנייה המסך מתריע ומציע שורת דחיסה. ‏1.4 מגהביט לשנייה נותן כ-15 מגה לדקה וחצי.
  ב-jsDelivr יש תקרה נפרדת של 20 מגה לקובץ.

---

## 5. באגים שכבר נפתרו — לא לחזור עליהם

1. **מסך שחור במקום המצלמה.** MindAR לוקח את ההורה של הסצנה כמיכל ותולה בו את וידאו המצלמה עם
   `z-index: -2`. כשההורה היה `<body>` בעל רקע אטום, הווידאו נצבע מתחת לרקע. הפתרון: הסצנה יושבת
   בתוך `<div id="ar">` ייעודי בגודל מסך מלא, עם כללי CSS שמחזיקים את הקנבס מעליו.
2. **כפתור המצלמה לא הגיב.** קריאה ל-`play()` על אלמנט וידאו בלי מקור נשארת תלויה לנצח ולא נכשלת.
   הפתרון: פתיחת חסימת הניגון רק אם יש מקור, ועם מגבלת זמן.
3. **"ספריית ההידור לא נטענה".** ראו סעיף 4 — מודול ES בתג script רגיל.
4. **גרסה ישנה בטלפון.** ה-service worker מגיש מטמון. יש כפתור "ניקוי מטמון ורענון" בחלון האבחון (ℹ),
   ואפשר גם לפתוח את הכתובת עם `?v=105`.
5. **יחסי וידאו.** הסרטון המקורי היה 720×1280 מול תמונה 914×1280, והיה נחתך. גם משקלו היה 6 מגה
   ב-4.7 מגהביט לשנייה — בזבזני. שורת הדחיסה:
   `ffmpeg -i in.mp4 -c:v libx264 -crf 26 -preset slow -vf scale=914:1280 -movflags +faststart -c:a aac -b:a 64k out.mp4`

---

## 6. אחסון המדיה

התנאי היחיד: השרת חייב להחזיר `Access-Control-Allow-Origin`, כי הווידאו נטען כטקסטורת WebGL.
**וואן־דרייב, גוגל דרייב ודרופבוקס לא מחזירים אותה ולכן אינם מתאימים**, גם עם קישור "ישיר".

| אפשרות | מגבלות |
|---|---|
| GitHub Pages (ברירת מחדל) | אתר עד 1 ג׳יגה, 100 ג׳יגה תעבורה בחודש |
| jsDelivr מעל אותו ריפו | 20 מגה לקובץ, מטמון 12 שעות על ענף |
| Cloudflare R2 / Supabase / Backblaze B2 | דורש הגדרת CORS ידנית |

נבחר בהגדרות מסך הגבאי, נשמר בשדה `cdn` שב-manifest. גם כתובת מלאה בשדה `video` של פריט בודד נתמכת.

---

## 7. אבטחה

מסך הגבאי מאחורי מסך כניסה. **הסיסמה אינה סוד מוחלט** — הדף ציבורי וכל אחד יכול לקרוא את הקוד.
היא נשמרת כטביעת אצבע SHA-256 של `שם:סיסמה` בקבוע `AUTH` שבתוך `admin.html`.

מה שכן מוגן באמת: **הטוקן של גיטהאב נשמר מוצפן** ב-AES-GCM, עם מפתח שנגזר מהסיסמה ב-PBKDF2
(‏250,000 סבבים). בלי הסיסמה אי אפשר להוציא אותו מהדפדפן. יש נעילה אוטומטית אחרי 30 דקות.

הטוקן צריך להיות Fine-grained, לריפו הזה בלבד, עם הרשאת Contents: Read and write.

להחלפת הסיסמה — לחשב טביעה חדשה בקונסולת הדפדפן ולהחליף את `AUTH`:

```js
crypto.subtle.digest('SHA-256', new TextEncoder().encode('user:pass'))
  .then(d => console.log([...new Uint8Array(d)].map(x => x.toString(16).padStart(2,'0')).join('')))
```

הריפו ציבורי, ולכן כל תמונה, סרטון ונוסח הקדשה גלויים לכל. אין להעלות דבר שאינו מיועד לפרסום.

---

## 8. תפעול שוטף

**הוספת תמונה:** מסך הגבאי → גרירת תמונה וסרטון → שם, מיקום, הקדשה → "בדיקת איכות זיהוי" →
"הוספה לרשימה" → "פרסום לגיטהאב". תוך דקה-שתיים זה בטלפונים.

**עדכון קוד:** מעלים את הקבצים ששונו דרך Add file → Upload files, מעלים את מספר הגרסה
בשלושה מקומות (`VERSION` ושורת הכיתוב ב-`index.html`, שם המטמון ב-`sw.js`, שורת הכיתוב ב-`admin.html`),
ופותחים בטלפון עם `?v=<גרסה>`.

**אזהרה:** לעולם לא להעלות `manifest.json` מחבילה מקומית מעל הרשימה החיה — היא תדרוס פריטים שפורסמו.

---

## 9. מצב נוכחי ומה הלאה

**עובד:** זיהוי ונעילה, הצמדה בזווית, הורדה לפי דרישה ושמירה במכשיר, מסך גבאי מלא מול גיטהאב,
כניסה מוגנת, תצוגה בוואטסאפ, שתי תמונות במקביל.

**עוד לא נעשה, רעיונות פתוחים:**

* מבחן קיר אמיתי בבית הכנסת — עד כה נבדק מול מסך מחשב. מסך מבריק וקשה למעקב; הדפסה עדיפה.
* כיול `filterMinCF` ו-`filterBeta` לפי איך שזה מרגיש בתאורה של ערבית מול שחרית.
* מעבר לאפליקציה מותקנת (ARCore/ARKit) אם ה-PWA לא מספיק יציב — מעקב חזק יותר, מחיר של חנויות אפליקציות.
* חיבור ליארצייטים של [[synagogue-display]] — התאמת ההקדשה ליום השנה.
* דחיסה אוטומטית של סרטונים בתוך מסך הגבאי (ffmpeg.wasm) במקום ההנחיה הידנית.
* תמיכה בכמה בתי כנסת: חבילה נפרדת עם לוגו וצבעים משלה.

---

## 10. שפה חזותית

רקע `#17111f`, זהב `#d6a22e` לרגע הנעילה ולכפתור אחד בכל מסך, טקסט `#f4efe4`.
ששת צבעי המגן מהלוגו משמשים לסימון מצב בלבד: `#7a2bd0`, `#f26a22`, `#1d86c9`, `#79b627`, `#e51d26`, `#2a4cb6`.
כותרות בפרנק־ריהל, ממשק באסיסטנט. ממשק המצלמה כהה כמעט לגמרי, כדי שהקיר האמיתי יהיה הדבר הבהיר במסך.
